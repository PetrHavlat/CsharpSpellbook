<UserControl x:Class="ToneGenerator.MvvmTest.MvvmTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToneGenerator.MvvmTest"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
			 d:DesignWidth="800">
    <Grid>
		<!--<Grid.DataContext>
			<local:PersonViewModel/>
		</Grid.DataContext>-->
		
	    <Grid.Resources>

        <!-- Styl pro TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="8"/>
			<Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        
						<ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">                                
                                <Setter Property="BorderBrush" Value="#4A90E2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#EEE"/>
                                <Setter Property="Foreground" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl pro Label -->
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        </Grid.Resources>

		
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock x:Name="TblTitle"
				   Grid.Row="0"
				   Text="Testování funkčnosti MVVM" 
				   FontSize="32"
				   Margin="20"/>

		<StackPanel Grid.Row="1"
					Margin="20">
			<Label x:Name="LblFirstName" Content="Jméno:"/>
			<TextBox x:Name="TbxFirstName" Text="{Binding Path=FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
			<Label x:Name="LblFamilyName" Content="Příjmení:"/>
			<TextBox x:Name="TbxFamilyName" Text="{Binding Path=FamilyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>					
		</StackPanel>

		<StackPanel Grid.Row="2"
					Margin="20">
			<Label x:Name="LblFullName" Content="Celé jméno:"/>
			<TextBox x:Name="TbxFullName" Text="{Binding Path=FullName, Mode=OneWay}" IsReadOnly="True"/>			
		</StackPanel>
	</Grid>
</UserControl>
